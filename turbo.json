{
  "$schema": "https://turbo.build/schema.json",
  "globalEnv": [
    "NODE_ENV",
    "DEMO_MODE",
    "DATABASE_URL",
    "AUTH_JWT_SECRET",
    "ANTHROPIC_API_KEY",
    "ANTHROPIC_MODEL",
    "API_BASE_URL",
    "WEB_ORIGIN",
    "WEB_PORT",
    "API_PORT",
    "STORAGE_DRIVER",
    "LOCAL_STORAGE_PATH"
  ],
  "tasks": {
    "build": {
      "dependsOn": ["^build"],
      "outputs": ["dist/**", ".next/**", "!.next/cache/**"]
    },
    "dev": {
      "cache": false,
      "persistent": true
    },
    "lint": {
      "dependsOn": ["^build"]
    },
    "typecheck": {
      "dependsOn": ["^build"]
    },
    "test": {
      "dependsOn": ["^build"]
    }
  }
}
