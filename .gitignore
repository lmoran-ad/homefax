node_modules/
.next/
dist/
out/
.turbo/
coverage/
*.tsbuildinfo

.env
.env.local
.env.*.local

storage/demo-uploads/*
!storage/demo-uploads/.gitkeep
# Anything a stray relative path created before the workspace anchoring landed.
apps/*/storage/
packages/*/storage/

playwright-report/
test-results/
blob-report/
.DS_Store

# TypeScript incremental build info
*.tsbuildinfo

# Local scratch
shots/

# Generated by `pnpm --filter @homefax/db export-sql` from the fixtures, hashes
# and all. Derived output, so it is regenerated rather than reviewed.
packages/db/seed.sql
